# dev backups & temp render/extract files
*.bak
_*.mjs
_*.html
_chk.mjs

# deps / os
node_modules/
.DS_Store
Thumbs.db

.vercel

# Bench render output: PNG/JSON/log artifacts regenerated by every harness run. Generated, never source.
bench/results/

# Phase -1 A-side: an untouched byte copy of the shipping file, regenerated on demand from git
game.baseline.html

# fal.ai key and generated reference/jumpscare media — API credentials and large binaries, never source
.env
assets/creature-ref/*.mp4
assets/creature-ref/*-frames.jpg
# and the raw in-game capture plates: half a megabyte each and regenerated by bench/tmp-wretch-ref.mjs on demand.
# The small JPEG reference plates that a generation is actually keyed to ARE committed, as the earlier creatures' are.
assets/creature-ref/*.png
